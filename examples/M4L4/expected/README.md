# Expected Output

Reference output files generated by running the example notebook.

These files can be used to check the format and scale of your results:
- `assemblies.yaml`: 34 assemblies
- `reactions.csv`: 3170 elementary reactions
- `classification_result.csv`: classification labels for each reaction

> **Note:** The order of entries may differ between runs due to a known issue in
> the enumeration algorithm. Content equivalence (same set of assemblies/reactions)
> is expected, but exact ordering is not guaranteed.
